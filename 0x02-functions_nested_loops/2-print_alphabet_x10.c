#include "main.h"
/**
 * print_alphabet_x10-entry of the program
 *
 * Return: always 0
 */
void print_alphabet_x10(void)
{
	int line, ch;

	for (line = 0; line <= 9; line++u)
	{
		for (ch = 'a'; ch <= 'z'; ch++)
		_putchar(ch);
	_putchar('\n');
	}
}

